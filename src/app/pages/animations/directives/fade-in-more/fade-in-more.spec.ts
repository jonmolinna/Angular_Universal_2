import { FadeInMore } from './fade-in-more';

describe('FadeInMore', () => {
  it('should create an instance', () => {
    const directive = new FadeInMore();
    expect(directive).toBeTruthy();
  });
});
